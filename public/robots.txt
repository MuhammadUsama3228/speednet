User-agent: *
Allow: /

# Block access to API routes for security
Disallow: /api/

# Allow crawling of all other assets (including _next) for proper rendering
# Googlebot needs access to JS/CSS to render the page correctly

# Sitemap
Sitemap: https://speednet.app/sitemap.xml
